{
  "$schema": "http://www.schemastore.org/package.json",
  "name": "cs2-rainbow-crosshair",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "lint": "tsc --noEmit && biome check",
    "lint:fix": "tsc --noEmit && biome check --write",
    "build": "bun run build.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.11",
    "@types/bun": "latest",
    "typescript": "^5.9.3"
  }
}
